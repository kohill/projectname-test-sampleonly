package com.healthedge.app.flow;

import com.healthedge.common.data.login.User;
import com.healthedge.app.providers.PageProvider;
import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class LoginFlow {

    private static final Logger LOG = LogManager.getLogger(LoginFlow.class);

    public LoginFlow(PageProvider pageProvider) {
        super();
    }

    public void login(User userData) {

    }

    public void logout() {

    }

}
