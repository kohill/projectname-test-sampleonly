package com.companyname.app.flow;

import com.companyname.common.data.login.User;
import com.companyname.app.providers.PageProvider;
import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class LoginFlow {

    private static final Logger LOG = LogManager.getLogger(LoginFlow.class);

    public LoginFlow(PageProvider pageProvider) {
        super();
    }

    public void login(User userData) {

    }

    public void logout() {

    }

}
